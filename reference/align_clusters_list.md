# Align multiple clusterings in list format

Align multiple clusterings in list format

## Usage

``` r
align_clusters_list(
  clust_list,
  clust_ref,
  clust_names = NULL,
  handle_unmatched = "label",
  verbose = FALSE
)
```
