# Align multiple clusterings in matrix format

Align multiple clusterings in matrix format

## Usage

``` r
align_clusters_matrix(
  clust_matrix,
  clust_ref,
  clust_names = NULL,
  handle_unmatched = "label",
  verbose = FALSE
)
```
