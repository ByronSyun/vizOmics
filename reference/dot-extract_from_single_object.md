# Extract Data from Single Experiment Object

Internal helper to extract data from a single experiment object based on
source.

## Usage

``` r
.extract_from_single_object(
  object,
  source,
  assay_name,
  reducedDim_name,
  features
)
```
